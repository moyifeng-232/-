<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.secondhand.mapper.ChatMessageMapper">
    
    <resultMap id="BaseResultMap" type="com.campus.secondhand.entity.ChatMessage">
        <id column="id" property="id" />
        <result column="sender_id" property="senderId" />
        <result column="receiver_id" property="receiverId" />
        <result column="content" property="content" />
        <result column="read_status" property="readStatus" />
        <result column="create_time" property="createTime" />
    </resultMap>
    
    <!-- 获取两个用户之间的对话记录 -->
    <select id="getChatHistory" resultMap="BaseResultMap">
        SELECT * FROM chat_message
        WHERE (sender_id = #{userId1} AND receiver_id = #{userId2})
           OR (sender_id = #{userId2} AND receiver_id = #{userId1})
        ORDER BY create_time ASC
    </select>
    
    <!-- 获取用户的所有对话对象 -->
    <select id="getChatUsers" resultType="java.lang.Long">
        SELECT DISTINCT
            CASE
                WHEN sender_id = #{userId} THEN receiver_id
                ELSE sender_id
            END as chatUserId
        FROM chat_message
        WHERE sender_id = #{userId} OR receiver_id = #{userId}
        GROUP BY chatUserId
    </select>
    
    <!-- 更新消息为已读 -->
    <update id="updateMessageReadStatus">
        UPDATE chat_message
        SET read_status = 1
        WHERE sender_id = #{senderId} AND receiver_id = #{receiverId} AND read_status = 0
    </update>
    
    <!-- 获取用户未读消息数量 -->
    <select id="getUnreadMessageCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM chat_message
        WHERE receiver_id = #{userId} AND read_status = 0
    </select>
</mapper>
